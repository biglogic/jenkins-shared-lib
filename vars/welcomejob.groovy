def call(String jenkins_pipeline_name, String jenkins_lib_name) {
                pipeline {
                  agent any
                   stages {
                      stage('Hello') {
                          steps {
                               sh "ls -al"
           }
       }
  
   }
}
}
